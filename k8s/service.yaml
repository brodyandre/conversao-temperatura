apiVersion: v1
kind: Service
metadata:
  name: conversao-temperatura
  labels:
    app: conversao-temperatura
spec:
  type: NodePort
  selector:
    app: conversao-temperatura
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
      nodePort: 30000
